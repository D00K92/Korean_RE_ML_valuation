.venv/
__pycache__/
*.py[cod]
*.egg-info/
.DS_Store
.env
*.key

# local data lake — never commit (data/* not data/, so negations below work)
data/*
!data/.gitkeep
# ...except bundled reference inputs (static lookup tables, rebuildable sources)
!data/reference/
!data/reference/**

# ml / tracking artifacts
mlruns/
mlartifacts/
*.duckdb
*.parquet
*.pkl

# airflow
logs/
airflow.db
airflow.cfg
standalone_admin_password.txt

*.log
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# raw transit reference downloads (bulky) — derived .parquet is force-added instead
data/reference/*_all_lines_*.csv
